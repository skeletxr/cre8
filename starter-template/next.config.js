/** @type {import('next').NextConfig} */
const nextConfig = {
  // Copy your configuration from next.config.ts here
  reactStrictMode: true,
  swcMinify: true,
  // Other config options...
};

module.exports = nextConfig;